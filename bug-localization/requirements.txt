# Core
pyyaml>=6
python-dotenv>=1
requests>=2.31        # Ollama HTTP API

# ROLE 2 — code graph
networkx>=3.2
rank-bm25>=0.2

# ROLE 3 — dataset + plots
datasets>=2.19        # SWE-bench Lite loader
matplotlib>=3.8

# Tests
pytest>=8

# Optional: only needed if provider is switched to "anthropic" in config.yaml.
# anthropic>=1.0
